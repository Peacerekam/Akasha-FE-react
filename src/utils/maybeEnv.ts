// probably dont change
export const BASENAME = "/";
export const MAINTENANCE_MODE = false;
export const SHOW_ADS = true;

// probably change
export const IS_PRODUCATION = false; // set to true for akasha.cv domain
export const TRANSLATION_VERSION = 0.285; // increment this when translation keys are outdated
